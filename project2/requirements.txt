# project2 dependencies
# RF gateway (pure C, no Python deps for core)
# Vision + Dashboard require:
numpy
opencv-python-headless
PyQt5
# Optional:
# psutil         # system resource monitoring (fallback to mock if missing)
# rknnlite       # RKNN model inference (RK3568 only, use mock_mode on PC)
