# project2 dependencies
# RF gateway (pure C, no Python deps for core)
# Vision bridge/runtime support on the WSL side:
numpy
opencv-python-headless
# Optional:
# rknn-toolkit2  # install from Rockchip official wheel when WSL-side model processing is needed
# rknnlite       # board-side runtime only; not for the Windows Qt simulator
